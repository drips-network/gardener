import MyComponent from '$lib/MyComponent.svelte';

// This is a conceptual usage, actual Svelte component usage
// would be within a Svelte file or compiled differently.
// For testing, we just care about the import resolution.
console.log(MyComponent);