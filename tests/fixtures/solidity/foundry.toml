[profile.default]
src = 'contracts'
out = 'out'
libs = ['lib', 'node_modules'] # Include node_modules for OpenZeppelin and other dependencies

# See more config options https://github.com/foundry-rs/foundry/tree/master/config